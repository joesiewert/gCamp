<% if record.errors.any? %>
  <div class="alert alert-danger">
    <strong><%= pluralize(record.errors.count, "error") %> prohibited this form from being saved:</strong>
    <ul>
      <% record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
